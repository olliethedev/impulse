# Triage label vocabulary

Canonical role names are `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. Check existing remote labels before applying or creating them. These defaults are documented; no remote label changes were needed to implement the project.
