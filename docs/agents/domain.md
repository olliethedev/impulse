# Domain documentation

Impulse has one context. Read `CONTEXT.md` for domain language and relevant decisions in `docs/adr/` before changing behavior. The glossary contains terminology; implementation requirements are in `docs/spec.md` and the CLI contract. Keep validation evidence separate in `docs/implementation.md`.
