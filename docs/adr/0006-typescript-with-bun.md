# Build Impulse in TypeScript with Bun

Use TypeScript with Bun because the maintainer is more familiar with TypeScript, making the codebase easier for them to maintain and review. Bun's standalone executable support fits the planned macOS, Windows, and Linux distribution model. Go was considered for the CLI and scheduler, but TypeScript familiarity determined the choice; toolchain versions and dependencies remain separate decisions.
